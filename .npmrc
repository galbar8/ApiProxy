# Workspace packages are linked from the workspace, never fetched from the registry.
link-workspace-packages=true
prefer-workspace-packages=true
# Fail fast instead of silently resolving an unexpected engine.
engine-strict=true
